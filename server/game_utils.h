#ifndef GAME_UTILS_H
#define GAME_UTILS_H

#include <QtNetwork>
#include <vector>
#include <sstream>
#include <vector>
#include <map>
#include <algorithm>

std::string get_command_type(std::string command);

#endif // GAME_UTILS_H
